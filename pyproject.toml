[tool.poetry]
name = "mbdevice"
version = "0.1.0"
description = ""
authors = ["MotRM"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
pytest = "^7.4.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
